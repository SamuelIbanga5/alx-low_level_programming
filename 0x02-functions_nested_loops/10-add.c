#include "main.h"
/**
 * add - Program that adds two integers and returns result.
 * @a: First operand
 * @b: second operand to be added.
 *
 * Return: result after addition.
 */
int add(int a, int b)
{
	int result = a + b;
	return (result);
}
